Use case: stylized-concept
Asset type: transparent end goal / exit gate map prop for Godot side-scroller
Primary request: Generate only the Ember Fields end goal / exit gate map prop. This is one interactive map prop / stage end goal only, not a UI icon and not a character/enemy/combat asset.
Input image context: use the visible Ember Fields start flag, checkpoint marker, and 32x32 volcanic tileset above for marker family continuity, volcanic stone material, pixel density, dark iron, ember-red palette, rune language, and side-scroller tile scale. The exit gate should clearly belong to the same family, but be the largest and most final marker.
Canvas/layout: one centered prop intended as 128x128 pixels. Stable bottom-center anchor. The prop should fit inside the frame with a little padding. It may be a compact volcanic ruin gate, portal arch, or lava-lit doorway, but must remain a single reusable map prop.
Chroma key: create the prop on a perfectly flat solid #ff00ff magenta chroma-key background for background removal. The background must be one uniform color with no shadows, gradients, texture, reflections, floor plane, or lighting variation. Do not use #ff00ff, magenta, purple, or violet anywhere in the prop art.
Style/medium: clean high-quality 2D pixel-art side-scroller prop, crisp readable silhouette, controlled texture detail, no blurry gradients, no glossy 3D rendering.
Visual details: volcanic ruin exit gate made of dark stone and dark iron, ember glow inside a doorway or portal slit, ember-red banner/flag elements matching the start and checkpoint markers, small orange rune or sigil, lava-lit base accents. Clearly reads as the end goal / stage exit; larger and more important than start and checkpoint markers.
Gameplay requirements: consistent with 32x32 tile scale, readable beside terrain, bottom-center anchor suitable for placement metadata, portal glow should be contained and not look like a combat VFX burst.
Avoid: boss body, monster face, character silhouette, projectile effects, giant full background temple, UI icon look, text, labels, arrows, wide platform base that looks like terrain.
Constraints: one prop only; no characters, no enemies, no boss body, no projectiles, no combat VFX, no full background scene, no UI, no text, no labels, no arrows, no debug markers, no watermark.

Postprocess note: The generated magenta-key source was background-removed, despilled, cropped to the gate silhouette, and normalized into a 128x128 transparent prop. Scale preview compares it against the accepted start flag, checkpoint marker, and a 32x32 tile grid.
