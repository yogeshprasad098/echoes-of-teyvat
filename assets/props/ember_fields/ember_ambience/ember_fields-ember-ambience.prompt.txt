Use case: stylized-concept
Asset type: transparent ambient ember particle object/layer for Godot map atmosphere
Primary request: Generate only the Ember Fields ember ambience object/layer asset. This is map ambience only, not a projectile, combat VFX, explosion, character, enemy, or background scene.
Input image context: use the visible Ember Fields lava surface and existing volcanic assets above for warm ember palette, pixel density, and subtle atmospheric style.
Canvas/layout: one compact ambience asset intended as 128x128 pixels. Show sparse controlled ember particles with larger readable pixel clusters, not tiny dust. Use about 28-40 ember motes total, with a few 2x2 and 3x3 bright cores, a few dim red ash flecks, and generous transparent space between clusters. More embers should sit in the lower half, with a few drifting upward.
Chroma key: create the asset on a perfectly flat solid #00ff00 chroma-key background for background removal. The background must be one uniform color with no shadows, gradients, texture, reflections, floor plane, or lighting variation. Do not use #00ff00 anywhere in the ember art.
Style/medium: clean high-quality 2D pixel-art ambience object, crisp readable pixels, warm orange/yellow embers, controlled density, no blurry gradients, no glossy rendering.
Visual details: tiny glowing ember motes, a few small ash flecks, warm orange and yellow core pixels with subtle red edges, no large fireballs, no burst shape, no ring, no shockwave.
Gameplay requirements: subtle enough not to hide 32x32 platform edges or player silhouettes, but readable when enlarged for preview and still visible at actual 128x128 asset scale. It should work as a low-opacity ambience layer/object in volcanic Ember Fields maps.
Constraints: map ambience only; no characters, no enemies, no boss body, no projectiles, no combat VFX, no explosion, no full background scene, no UI, no text, no labels, no arrows, no debug markers, no watermark.

Postprocess note: The first accepted 128x64 version was too compressed and did not preserve the raw generation. This regenerated version was background-removed, despilled, and normalized by resizing the full raw composition into a 128x128 transparent ambience object so the final asset and preview match the raw layout more closely.
