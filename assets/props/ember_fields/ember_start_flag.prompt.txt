Use case: stylized-concept
Asset type: transparent player start marker map prop for Godot side-scroller
Primary request: Generate only the Ember Fields start flag map prop. This is one compact map prop only, not a UI icon and not a character/enemy/combat asset.
Input image context: use the visible Ember Fields stage reference and 32x32 volcanic tileset above for marker family direction, volcanic stone material, pixel density, dark iron, ember-red palette, and side-scroller tile scale.
Canvas/layout: one centered compact prop intended as 64x96 pixels. Stable bottom-center anchor. The prop should fit inside a narrow vertical frame: about 1 to 1.5 tiles wide at the base, not a wide platform. Leave a little padding around the top and sides.
Chroma key: create the prop on a perfectly flat solid #ff00ff magenta chroma-key background for background removal. The background must be one uniform color with no shadows, gradients, texture, reflections, floor plane, or lighting variation. Do not use #ff00ff, magenta, purple, or violet anywhere in the prop art.
Style/medium: clean high-quality 2D pixel-art side-scroller prop, crisp readable silhouette, controlled texture detail, no blurry gradients, no glossy 3D rendering.
Visual details: simple dark iron pole, small tattered ember-red banner/flag, small orange ember mark on the banner, compact volcanic stone base or cracked stone foot, subtle ember accents. It should read clearly as the player start marker but be modest and less dramatic than a checkpoint shrine or exit gate.
Gameplay requirements: consistent with 32x32 tile scale, readable beside terrain, not oversized, not too ornate, bottom-center anchor suitable for placement metadata.
Avoid: wide platform bases, huge gates, shrine shapes, large gold spearheads, oversized banners, extra props, text, labels.
Constraints: one prop only; no characters, no enemies, no boss body, no projectiles, no combat VFX, no full background scene, no UI, no text, no labels, no arrows, no debug markers, no watermark.

Postprocess note: The first generated start flag had a wide platform base and was rejected. The accepted compact source was background-removed, despilled, cropped to the flag silhouette, and normalized into a 64x96 transparent prop.
