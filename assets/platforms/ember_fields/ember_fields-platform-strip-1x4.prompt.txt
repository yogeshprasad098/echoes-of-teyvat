Use case: stylized-concept
Asset type: transparent side-scroller platform strip sheet for Godot map assets
Primary request: Generate only the Ember Fields platform_strip_1x4 sheet. This is not an animation and not a square prop pack.
Input image context: use the visible Ember Fields 32x32 tileset and stage reference above for volcanic stone material, warm lava underside accent, pixel density, palette, and readable platform top style.
Canvas/layout: one horizontal strip with four equal cells, left to right: 1 left cap, 2 middle repeat, 3 right cap, 4 broken/corner variant. Each piece is intended as 64x32 pixels. Keep the pieces aligned to a shared baseline and shared walkable top height. Leave clear padding between cells so they can be sliced.
Chroma key: create the strip on a perfectly flat solid #00ff00 chroma-key background for background removal. The background must be one uniform color with no shadows, gradients, texture, reflections, floor plane, or lighting variation. Do not use #00ff00 anywhere in the platform art.
Style/medium: clean high-quality 2D pixel-art side-scroller platform asset, crisp pixel clusters, strong collision-friendly silhouette, no blurry gradients, no glossy 3D rendering.
Materials: dark volcanic stone blocks, warm gray highlighted walkable top edge, deep brown underside, small ember cracks, subtle lava-orange glow under the platform.
Gameplay requirements: top edge must be a clear horizontal standable surface; left/middle/right pieces must tile cleanly into one longer platform; middle repeat must be seamless on left and right edges; bottom silhouette can have small volcanic chips but should not confuse collision. The broken/corner variant may have a chipped end but must still align to the same top edge.
Constraints: no characters, no enemies, no boss body, no projectiles, no combat VFX, no UI, no text, no labels, no arrows, no debug markers, no watermark, no full background scene, no square prop pack.

Postprocess note: The generated chroma-key source was background-removed and normalized into a strict 256x32 strip with four 64x32 cells.
