class_name ReactionBurstSpawner
extends Node
## Singleton-style helper. Phase ε will hand it three GPUParticles2D presets;
## for now this is a no-op so calls compile.

static func play_at(_world_position: Vector2, _reaction: int) -> void:
	pass
